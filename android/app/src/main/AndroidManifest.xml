<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!--
        REQUIRED for AppsFlyer attribution on Android 13+ (API 33).
        Without this permission Google Play Services returns a zero GAID
        ("00000000-0000-0000-0000-000000000000") and every install is
        seen by AppsFlyer as "Organic" (media_source=null, campaign=null)
        — which in turn causes `/config.php` to answer `ok:false` and
        the router to commit the install to native (white) mode.
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application
        android:label="ThunderPeak"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:extractNativeLibs="false"
        tools:replace="android:allowBackup,android:fullBackupContent,android:dataExtractionRules">
        <!--
            launchMode="singleTask" is important for FCM: the
            notification tap PendingIntent uses FLAG_ACTIVITY_NEW_TASK,
            and with the DEFAULT task affinity (= package name) Android
            reuses the existing task via onNewIntent instead of
            spawning a fresh Flutter engine. Do NOT re-add
            `android:taskAffinity=""` — that forces a full reload
            every time a push notification is tapped in the
            background or foreground.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:alwaysRetainTaskState="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--
                AppsFlyer OneLink deep-link intent filter.

                `autoVerify` is intentionally OFF. AppsFlyer does NOT
                host the required `/.well-known/assetlinks.json` on
                `thunderpeak.onelink.me` (verified: HTTP 404
                domain_not_found), so Android's App Links verification
                fails with state 1024 and — on API 31+ — our app is
                SILENTLY EXCLUDED from the intent chooser. With
                autoVerify off, our app becomes a regular chooser
                option again and can catch the OneLink URI itself.
                The router treats an inbound onelink.me URI with
                attribution query params as a Non-organic proof and
                skips AppsFlyer + gate-attribution entirely, POSTing
                a synthesized body straight to /config.php.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="thunderpeak.onelink.me" />
            </intent-filter>

            <!--
                Custom-scheme deep-link. Works regardless of Android
                App Links verification and regardless of AppsFlyer's
                domain configuration. Configure the OneLink template
                on the AppsFlyer dashboard to redirect to
                `thunderpeak://open?...<all-onelink-params>...` via
                the `android_url_scheme` / `af_dp` param, and every
                click will hand the attribution over to our app.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="thunderpeak" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <!--
            [FINGERPRINT] Project-unique channel id. Must EXACTLY match
            `kBeaconChannelId` in lib/wires/bolt_beacon.dart. Never reuse
            across projects.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="thunder_beacons" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- DebugKit "Open in Chrome" needs to see the Chrome package
             directly on Android 11+ (package visibility restrictions). -->
        <package android:name="com.android.chrome"/>

        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="market"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
